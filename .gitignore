# OS
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# Editor / IDE
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
*.swo
*~
*.sublime-workspace
*.code-workspace

# Logs
*.log
npm-debug.log*

# Dependências (não versionadas — projeto não usa build/bundler, mas ferramentas
# de dev como `bunx serve`/`npx serve` podem gerar cache local)
node_modules/
package-lock.json
bun.lockb

# Skills de agentes de IA - conteúdo vendorizado/reproduzível a partir de
# skills-lock.json (que permanece versionado); não faz parte do site em si.
.agents/skills/*
!.agents/skills/brand-identity/
!.agents/skills/brand-identity/**

# Variáveis de ambiente / segredos
.env
.env.*.local

# Arquivos temporários
*.tmp
*.zip
